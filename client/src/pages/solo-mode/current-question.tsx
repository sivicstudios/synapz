import CurrentQuestion from "../../components/solo-mode/CurrentQuestion";

export default function SoloModeCurrentQuestion() {
  return (
    <div className="container flex items-center justify-center w-full">
      <CurrentQuestion />
    </div>
  );
}